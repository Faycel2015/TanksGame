# This .gitignore file should be placed at the root of your Unity project directory
# Get latest from https://github.com/github/gitignore/blob/main/Unity.gitignore

# Unity Generated Directories
# These directories are generated by Unity and do not need to be tracked.
[Tt]emp/
[Ll]ibrary/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/

# Unity3D generated meta files
# Meta files are automatically created by Unity and should be ignored
*.pidb.meta
*.pdb.meta
*.mdb.meta

# Memory Captures (can get excessive in size and contain sensitive data)
[Mm]emoryCaptures/

# Recordings (can get excessive in size)
[Rr]ecordings/

# Uncomment this line if you wish to ignore the Asset Store Tools plugin
# /[Aa]ssets/AssetStoreTools*

# JetBrains Rider plugin files
# These are editor-specific files for JetBrains Rider that can be safely ignored
/[Aa]ssets/Plugins/Editor/JetBrains*

# IDE Files
# Ignore IDE-specific files such as Visual Studio or Rider files
.vs/
.idea/
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Gradle cache directory for Android builds
# Ignore the Gradle cache that can be recreated
.gradle/

# Crashlytics generated files for mobile development
# Ignore Crashlytics related build properties files
crashlytics-build.properties

# Temporary files for Android build
# Ignore temporary Android build files, especially large ones
[Aa]ssets/[Ss]treamingAssets/aa.meta
[Aa]ssets/[Ss]treamingAssets/aa/*

# Packed Addressables data files (ignore large binary assets)
# Addressable assets can be large and should be ignored by version control
[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*

# Unity3D generated file on crash reports
# Ignore Unity crash logs and system info
sysinfo.txt

# Operating System Specific Files
# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Windows system files
Thumbs.db
ehthumbs.db
Desktop.ini

# User-Specific Preferences
# User-specific settings and project preferences for Unity Hub
.local/

# Do not ignore Unity's LFS-tracked files (such as .unity, .prefab, .asset)
# Uncomment the lines below to ensure Unity's asset files are tracked by LFS
# **/*.asset
# **/*.unity
# **/*.prefab

# Git LFS configuration for large files
# Unity often uses large assets that should be tracked by Git LFS (Large File Storage)
# Common file types to be tracked by Git LFS
*.psd
*.tga
*.fbx
*.mp4
*.avi
*.mov
*.obj
*.wav
*.mp3
*.png
*.jpg
*.zip
*.rar
*.tar.gz
*.tar.bz2
*.unitypackage
*.apk
*.aab

# Optional: Ignore certain build files or platform-specific packages
*.unitypackage
*.unitypackage.meta
*.app

# Ignore large files that exceed GitHub's size limits
# These files should be tracked by LFS as they exceed the standard Git file size limits
# Add more file extensions if necessary (e.g., .dae, .3ds, etc.)
# Large asset types should be tracked by Git LFS

# Ignore temporary or generated files
# Ignore files that are usually created during development but are not needed in version control
*.bak
*.swp
*.swo
*.tmp
*.log

# Ignore bundled libraries or external dependencies
# Specifically ignore the Unity package cache to avoid committing these files to the repository
Tanks/Library/PackageCache/*
